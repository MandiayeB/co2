{
  "name": "co2-api",
  "version": "1.0.0",
  "description": "CO2 emissions API",
  "main": "./build/index.js",
  "scripts": {
    "start": "npm run build && node ./build/index.js",
    "pre-build": "rimraf ./build",
    "build": "npm run pre-build && tsc",
    "dev": "nodemon ./src/index.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Mandiaye Badiane",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.24.0",
    "csvtojson": "^2.0.10",
    "dotenv": "^10.0.0",
    "express": "^4.17.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.13",
    "@types/node": "^16.11.11",
    "nodemon": "^2.0.15",
    "rimraf": "^3.0.2",
    "ts-node": "^10.4.0",
    "typescript": "^4.5.2"
  }
}
